// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xsolution.h"

extern XSolution_Config XSolution_ConfigTable[];

XSolution_Config *XSolution_LookupConfig(u16 DeviceId) {
	XSolution_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XSOLUTION_NUM_INSTANCES; Index++) {
		if (XSolution_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XSolution_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XSolution_Initialize(XSolution *InstancePtr, u16 DeviceId) {
	XSolution_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XSolution_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XSolution_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

